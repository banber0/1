#批处理系统
##执行user代码报错 版本问题 
    cargo install cargo-binutils --locked
    强制指定版本
##编译应用程序，可以看到bin的.rs文件，在release文件里可以看到.bin文件
        link_app.S代码中有个疑问，为什么第八行和第九行没有对应的end，然后阅读可知：数组中相邻两个元素记录了每个应用程序的始末位置
        .S文件为汇编文件：
            .s (小写)：被视为“纯”汇编代码，编译器会直接把它交给汇编器（Assembler）处理。
            .S (大写)：编译器在把它交给汇编器之前，会先运行 C 预处理器（CPP）。


在跟着文档并看底层代码后，对于os的理解更加深入，对一些文本理论知识更深刻，接下来继续学习第三章内容